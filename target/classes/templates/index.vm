#parse('templates/header.vm')

<p>Welcome to the CS 440 Database web project.</p>

<p>You can use the navigation above to look at various entities in the chinook.db</p>

<h3>Project Requirements</h3>

<p>Below are the requirements for the CS440 Project, broken out by section.</p>

<h4>Artists</h4>
<ul>
    <li>All CRUD operations</li>
    <li>Implement Paging</li>
    <li><code>Artist.verify()</code></li>
    <li>Show all albums on the artist details page, with links to their album</li>
</ul>

<h4>Albums</h4>
<ul>
    <li>All CRUD operations</li>
    <li>Implement Paging</li>
    <li><code>Albums.verify()</code></li>
    <li>Show all tracks on the album details page, with links to the tracks</li>
</ul>

<h4>Tracks</h4>
<ul>
    <li>All CRUD operations</li>
    <li><code>Tracks.verify()</code></li>
    <li>A Simple Search that includes artist name and album title in the search criteria</li>
    <li>Implement advanced search on all the fields</li>
    <li>Tracks should include Artist and Album name in display, in a query-efficient manner</li>
    <li>Implement "Order By" headers in list view, clicking on them should order them in </li>
    <li>Extra Credit: Make Simple Search Live Search</li>
</ul>

<h4>Playlists</h4>
<ul>
    <li>Display only, both list and detail views</li>
    <li>Detail view should show associated tracks, paged</li>
</ul>

<h4>Employees</h4>
<ul>
    <li>All CRUD operations</li>
    <li>Implement Paging</li>
    <li><code>Employees.verify()</code></li>
    <li>Detail view should show boss, Reports and Customers (no paging)</li>
    <li>Employee Report 1: An employee tree (written inefficiently currently, optimize to issue only one query)</li>
    <li>Employee Report 2: A sales leader report: each employee with the number of invoices associated sorted
    most to least</li>
</ul>

<h4>Customers</h4>
<ul>
    <li>Display only, both list and detail views</li>
    <li>Detail view should show associated invoices, paged</li>
</ul>

<h4>Invoices</h4>
<ul>
    <li>Display only, both list and detail views</li>
    <li>Detail view should show associated invoice items, paged</li>
    <li>Invoice items should include track name, album name, artist name</li>
</ul>

#parse('templates/footer.vm')
